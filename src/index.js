import {
  iniciar,
} from './changes.js';

import {
// removePokemons,
// countPages,
// showPokemon,
// $buttonBack,
// $buttonNext,
} from './ui.js';

async function reload() {
  // spendPokemon();
  await iniciar();
  // cardsPokemon, spendPokemon;
}

reload();
